generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Connection URLs are configured outside schema for Prisma v7+
  // See https://pris.ly/d/config-datasource
}

// Add your data model definitions here
